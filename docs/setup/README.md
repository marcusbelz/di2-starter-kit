# Setup Guides

> One-time configuration/setup walkthroughs (local dev, server/deploy, CI, service accounts). Naming:
> `setup-XXX-<kebab-slug>.md` (numbered in the order a new contributor follows; never reassigned).
> See `.claude/rules/documentation.md`.

## Guides
- [Setup-001 (Example): PostgreSQL 17 in a Docker container](setup-001-example-postgres-17-docker.md) — _Example guide; adapt/replace when setting up the real project._
- [Setup-002 (Example): Read-only role on all schemas of a database](setup-002-example-readonly-role-all-schemas.md) — _Example guide; adapt/replace when setting up the real project._
